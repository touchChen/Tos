1、先假设进程运行在ring1级上，从内核进入进程，即从ring0转向ring1。
高特权级到低特权级是通过ret实现，这里用了iretd; 发生中断过程，压栈顺序 ss esp  eflag cs eip;执行iretd指令，出栈是压栈的逆过程
在进程里用到了ldt





进程

lea 指令，获取变量值的地址

c语言中的2位，是不是在汇编中占4位，，反汇编看看

保护模式下 中断与异常
